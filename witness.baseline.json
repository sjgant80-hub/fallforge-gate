[
  {
    "mutation": "< → <=",
    "snippet": "for (let t = 16; t < 64; t++) {",
    "reason": "Genuinely equivalent: the mutated bound runs one extra schedule iteration that writes w[64], out of bounds on the fixed Uint32Array(64) — typed-array OOB writes are silently discarded, the loop body has no other side effect, and nothing reads past w[63]. Behaviour is bit-identical; the FIPS vectors pass either way."
  },
  {
    "mutation": "< → <=",
    "snippet": "for (let i = start; i < output.length; i++) {",
    "reason": "Genuinely equivalent: the mutated bound runs one extra iteration reading output[output.length], which is undefined — undefined matches none of the branch characters (backslash, quote, braces), no state changes, and the loop then exits exactly as before. Behaviour is identical for every input."
  }
]
